<main class="content-wrapper">
    <h1 class="section-title">Survey detail</h1>
    <ul class="projects-list w-list-unstyled">
    {{#each survey}}
      <li class="project-card">
        <a class="card-link w-inline-block" href="/surveyspage">
          <div class="survey-detail">
            <div class="card-title">Survey information :</div>
            <div class="project-card-title">{{this.surveyNme}}</div>
            <div>{{this.surveyComm}}</div>
            <div class="project-card-footer">
              <div class="project-card-dates">{{this.surveyStart}} &#x279E; {{this.surveyEnd}}</div>
              <div class="project-card-id">#{{this.surveyId}}</div>
            </div>
          </div>
          <div class="survey-site">
            <div class="card-title">Site Description :</div>
            <div class="project-card-title">{{this.siteName}}</div>
            <p>{{this.siteDesc}}</p>
            <div class="gps-location">
              <div>Lat : {{this.lat}}</div>
              <div>Long : {{this.long}}</div>
            </div>
            <div class="project-card-footer">
              <div class="project-card-dates">Accuracy : {{this.accu}} m</div>
              <div class="project-card-id">#{{this.siteId}}</div>
            </div>
          </div>
        </a>
      </li>
    {{/each}}
    </ul>
  </main>